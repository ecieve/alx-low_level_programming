#include "main.h"
/**
*function that computes the absolute value of an integer.
*Return: the absolute value of int
*
*/
int _abs(int num) {
    if (num < 0) {
        return -num;  
    } 
else 
{
        return num;   
}
}
